# 한국관 — 이 저장소에서 일하는 규칙

사장님(Younsoo)은 **여러 세션을 동시에** 쓴다. 클라우드 세션, 맥의 Claude
Code, 다른 창. 그래서 이 저장소는 한 사람이 혼자 쓰는 저장소처럼 굴면
반드시 어긋난다. 아래 두 가지는 매 세션 지켜야 한다.

## 1. 시작할 때와 커밋 직전에 다른 세션 것을 확인한다

작업을 시작하기 전에:

```bash
git fetch origin
git log --oneline -5            # 내가 모르는 커밋이 있나
git log --oneline -3 origin/main
```

**커밋 직전에 한 번 더 본다.** 내가 파일을 고치는 동안 다른 세션이 같은
파일에 커밋을 올렸을 수 있다. 실제로 한 세션 안에서 네 번 일어났다.

모르는 커밋이 있으면 그 위로 올린다. 되돌리거나 무시하지 않는다:

```bash
git stash            # 또는 내 변경을 patch 로 빼둔다
git reset --hard origin/main   # 필요하면 상대 커밋까지 받아온 뒤
git stash pop        # 3-way 로 다시 얹고 충돌을 푼다
```

`package.json` 의 `test` / `test:e2e` 목록은 **양쪽 것을 합친다.** 한쪽으로
덮으면 상대가 새로 넣은 테스트가 조용히 안 돌게 된다.

## 2. 푸시와 배포는 사장님이 한다

`git push` 하지 않는다. 커밋까지만 하고, 몇 개 대기 중인지 알려준다.
배포도 마찬가지다.

## 클라우드 세션 ↔ 맥 미러링

클라우드 샌드박스에서 작업했으면 맥에도 같은 커밋을 넣어야 한다. 맥에서는
절대 푸시하지 않는다.

1. 클라우드: `git format-patch -1 --binary -o /tmp/out`
2. `SendUserFile` → `device_commit_files` 로 맥의 `_to_delete/` 에 넣는다
3. 맥에서 적용한다. 이 마운트는 `git apply --index` 가 자주 실패하고
   `.git` 안의 lock 파일을 지우지 못하므로, 아래 래퍼를 쓰고 작업 파일에만
   적용한 뒤 **패치가 건드린 파일만** 골라 add 한다:

```bash
g() {
  for f in .git/HEAD.lock .git/index.lock .git/packed-refs.lock \
           .git/ORIG_HEAD.lock .git/refs/heads/*.lock .git/logs/*.lock; do
    [ -f "$f" ] && mv "$f" "$f.st_$(date +%s%N)_$RANDOM" 2>/dev/null
  done
  git "$@"
}
g apply "$D/patch"
FILES=$(g apply --numstat "$D/patch" | awk '{print $3}')
g add -A -- $FILES        # git add -A 만 쓰면 사장님의 로컬 변경까지 들어간다
g commit -F "$D/full"
```

4. **트리 해시로 검증한다.** 커밋 메시지가 같아도 내용은 다를 수 있다:

```bash
git rev-parse HEAD^{tree}   # 양쪽에서 같은 값이어야 한다
```

`Qr/app/package-lock.json` 은 맥에 늘 수정된 채로 있다. 우리 커밋에
딸려 들어가지 않게 한다.

## 인원과 메뉴는 하나의 세트다

사장님(2026-09-10, 두 번째로 말씀하심):

> "완전 포장(counter qr)를 제외하고 모든 테이블들은 인원과 메뉴는 하나의
> 세트야. 삭제되던 결제가 완료되던, 자리 이동을 하던 합산을 하던 같이
> 움직이는 하나야."

이건 화면 표시 규칙이 아니라 데이터 규칙이다. 어느 한쪽만 움직이는 코드는
쓰지 않는다.

- 결제가 끝나면 둘 다 사라진다
- 자리를 옮기면 둘 다 옮겨간다 (`movePartySize`)
- 합산 결제도 테이블마다 똑같이 정리된다
- 살아 있는 주문이 있는데 인원이 없는 자리는 **있을 수 없는 상태**다.
  그 자리 QR 은 앉아 계신 손님에게 인원을 다시 묻고, 결제 화면은 빈 자리로
  보여준다. `partyOfTable()` 이 읽을 때 둘을 맞춰보고 고쳐 놓는다.
- 포장 카운터(`is_counter`)만 예외다. 거기 쌓이는 주문은 서로 무관한 손님
  것이라 「이 자리에 몇 명」이라는 말 자체가 성립하지 않는다.

규칙은 `src/partySize.js` 한 곳에 있다. 인원수를 만지는 코드를 다른 파일에
새로 쓰지 않는다 — 흩어지면 「결제했을 때만 비운다」가 조용히 무너진다.
`test/party-set.test.js` 가 이 규칙을 잰다.

## 주방이 알아야 할 변화는 전부 종이로 나간다

사장님(2026-09-10):

> "테스터 뿐만이 아니라 저렇게 늘 출력이 되어야 해. 수기던 고객이 직접
> 주문을 하던 자리 옮김이던."

빌지가 안 나오면 음식이 안 나간다. 화면에만 뜨고 종이가 안 나오는 경로는
「조금 불편한」 것이 아니라 **주문을 잃는 것**이다.

지금 상태:

| 경로 | 인쇄 |
|---|---|
| 손님이 QR 로 주문 | ○ |
| 직원 수기 주문 | ○ (손님 주문 화면을 그대로 열어 `POST /api/orders` 로 간다) |
| 테스터 모드 주문 | ○ (참여한 기기에 한해 — 인쇄 코드에 테스트 예외가 없다) |
| 품목 추가·수정 | ○ (바뀐 줄만 「추가 / 취소」 알림 빌지 한 장) |
| 자리 옮김 | ○ (「7 → 9」 알림 빌지 한 장) |

알림 빌지는 새 주문 빌지와 다르다. 한 장만 나가고(주방용), 주문 상태를
밀지 않는다. 합계도 안 찍는다 — 바뀐 부분만 적힌 종이에 합계가 있으면
「이만큼만 받으면 되는」 것으로 읽힌다. 같은 변경을 두 번 찍지 않도록
`hg_admin_printedNotices` 에 「주문번호@변경시각」으로 남긴다.

새 경로를 만들 때는 「이게 주방에 종이로 가는가」를 먼저 본다. 화면 갱신만
하고 끝나는 코드는 이 규칙을 어기는 것이다.

인쇄가 안 될 때 **화면이 이유를 말해야 한다.** 2026-09-10 하루에만 세 번,
자동 인쇄가 조용히 아무 일도 안 해서 원인을 찾는 데 매번 한참 걸렸다
(담당 기기가 LAN 밖, 참여 안 한 기기, 밀린 주문 몰아치기 방지). 조용히
넘어가는 분기를 새로 만들지 않는다.

## 인쇄 담당 기기

`store.settings.print_device` 는 「지금 어느 기기가 빌지를 뽑는가」다.
자동 인쇄는 담당 기기에서만 돈다(`printHereAllowed`). **수기 인쇄는 이
검사를 안 거친다** — 그래서 「수기로는 나오는데 자동으로는 안 나온다」가
나오면 십중팔구 담당이 딴 기기에 잡혀 있는 것이다.

프린터에 닿지 못하는 기기(가게 LAN 밖)가 담당을 가져가면 아무도 못 찍는
상태가 된다. 테스터 모드 종료가 이 값을 되돌리지 않게 해 뒀다
(`SNAPSHOT_SKIP`).

## store 문서를 통째로 쓰지 않는다

`save()` 는 store 문서 **전체**를 지금 이 인스턴스가 들고 있는 값으로
갈아끼운다. Vercel 은 인스턴스를 여러 개 띄우고, 요청마다 `refreshStore()`
로 새로 읽어도 그 뒤에 다른 요청이 저장을 마치면 그걸 덮어쓴다.

2026-09-10 점심에 이것 때문에 세 가지가 한꺼번에 터졌다 — 결제로 지운
인원수가 되살아나고, 주문 번호 카운터가 뒤로 가서 번호가 겹치고, 겹친
번호 때문에 앞 주문이 덮어써지고 빌지가 안 찍혔다.

자주 오가는 길에서는 문서를 통째로 쓰지 않는다.

- 주문 번호: `reserveId()` — 데이터베이스에서 `$inc` 로 받아온다
- 인원수: `savePartySize()` / `patchArrayItem()` — 그 자리 네 칸만
- 그 밖의 작은 값: `saveFields()`

새 기능에서 `save()` 를 부르기 전에, 그 값이 자주 바뀌는 것인지 먼저 본다.

## 비밀값

Firebase 서비스 계정 키는 Vercel 환경변수(`FIREBASE_SERVICE_ACCOUNT`)에만
둔다. 저장소에도, 관리자 설정 화면에도 넣지 않는다. `.env` 를 들여다볼 때
값은 절대 찍지 않는다 — 키 이름과 길이까지만.

## 테스트

```bash
cd Qr/app
npm test        # 유닛
npm run test:e2e   # 브라우저(Playwright), 오래 걸린다
```

새 기능에는 테스트를 같이 넣는다. 이 저장소의 테스트는 "동작하는가" 보다
**증상의 원인을 재는** 쪽에 가깝다 — 화면 높이, 글자 크기 비율, 명령 하나의
바이트 수처럼. 사장님이 겪은 문제를 그 자리에서 다시 잡을 수 있어야 한다.
